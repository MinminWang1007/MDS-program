In this folder, there are a total of 4 cpp files.

filter path.cpp -the  program is   filter paths which induce matrices that always contain 0.

MDS .cpp and Involutory MDS. cpp - the two programs are find 4*4 MDS matrix with 35xor gates and    4*4 Involutory MDS matrix with 36xor gates.


The testing is performed with a C++ program, running Windows.